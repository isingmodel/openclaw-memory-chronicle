# 8장. 설계 전환과 2025년 제안의 재검토

2장부터 7장까지는 작은 파일 관례가 검색, 수명주기, 플러그인 소유권, 대화 기록, 보호된 대화 간 회상으로 자라난 시간을 따라갔다. 마지막 대화 간 회상은 아키텍처 기준일에는 `main`에만 있었고, 이후 `v2026.7.2-beta.3`과 `v2026.7.2-beta.4`에서 단계적으로 출시되었다. 이제 연대기에서 한 걸음 물러나, 그 변화들이 반복해서 선택한 설계 방향을 읽을 차례다. 개별 기능의 목록보다 중요한 것은 문제가 생길 때마다 책임이 어디로 이동했는가다.

가장 큰 흐름은 프롬프트에 맡긴 희망을 런타임의 강제로, 흩어진 사이드카를 소유권이 분명한 SQLite 상태로, 경계 없는 회상을 범위를 따지는 검색으로 바꾼 과정이었다. 동시에 사람에게 보이는 의미 메모리와 기계가 운영하는 상태를 갈라 놓았다. 이 세 전환은 서로 다른 시기에 일어났지만, 결국 같은 질문에 답한다. 기억의 정확성·복구 가능성·안전성을 누가 책임지는가?

다만 책임이 런타임으로 이동했다는 사실만으로 그 변화가 영구적으로 우월하다고 결론 내릴 수는 없다. 권한·데이터 보존처럼 모델 성능과 무관하게 지켜야 할 결과 계약이 있는 반면, 강제 검색·고정된 도구 순서처럼 특정 시기의 모델 능력을 보완하려고 넣은 실행 전략도 있다. 전자는 유지해야 하지만 후자는 모델과 작업 분포가 바뀔 때 효용을 다시 입증해야 한다. 이 장은 역사에서 확인된 방향과 앞으로 채택할 규범을 이 지점에서도 구분한다.

이 장의 앞부분은 2026-07-18 기준으로 역사에서 확인되는 설계 원칙을 정리한다. 뒤의 2025년 제안 비교는 당시 구상이 실제 시스템에 얼마나 남았는지를 돌아보는 평가다. 아직 미완성인 의미 모델을 더 우선해서 평가해야 한다는 결론은 유지보수자가 채택한 로드맵이 아니라, 이후 장에서 결과 비교로 검증할 이 책의 문제 제기다.

[← 이전: 2026년 7월](07-july-2026.md) · [목차](README.md) · [다음: 신호와 평가 전략 →](09-signal-ledger-and-evaluation.md)

## 무엇이, 어떻게, 왜 달라졌는가

다음 표는 기능별 연표가 아니다. 각 행을 왼쪽에서 오른쪽으로 읽으면 하나의 설계 압력이 초기 형태를 어떻게 밀어냈고, 어떤 소유권과 실행 경계로 바뀌었으며, 왜 그 변화가 필요했는지가 드러난다. “이후 형태”는 모든 메모리 플러그인에 공통인 보편 계약이 아니라 해당 변화가 자리 잡은 경로를 요약한다.

| 설계 압력                                       | 초기 형태                                                    | 이후 형태                                                                                                                           | 프로젝트가 바뀐 이유                                                                                                    |
| ----------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 컨텍스트가 사라진다                             | 에이전트에게 일별 메모를 쓰라고 지시                         | 하트비트 유지보수, 컴팩션 여유분, 컴팩션 전 플러시, 재설정 훅, 결정적 시작 컨텍스트                                                 | 명시적인 컨텍스트 손실·덮어쓰기 실패가 프롬프트 지침만으로는 수명주기를 보장할 수 없음을 보여 주었다.                   |
| 커진 파일은 검색하기 어렵다                     | 오늘·어제 메모와 선별된 루트 메모리를 읽음                   | 벡터 검색, FTS5, 하이브리드 점수화, MMR, 시간 감쇠, [QMD](01-current-architecture.md#qmd-explained), CJK 토큰화, 멀티모달 추가 경로 | 관련 증거가 있어도 싸고 정확하게 찾기 어려웠고, 언어·공급자 실패가 집계 지표의 맹점을 드러냈다.                         |
| 모델은 도구를 안정적으로 호출하지 않는다        | 프롬프트가 모델에게 검색을 요청                              | 런타임의 시작 시점 주입과 선택적 Active Memory 응답 전 하위 에이전트                                                                | 도구가 있다고 모델이 반드시 쓰는 것은 아니었고, 첫 답변 회상에는 결정적 단계가 필요했다.                                |
| 하나의 구현이 모든 경우에 맞지 않는다           | 에이전트 코어 안의 내장 메모리 동작                          | 배타적 메모리 슬롯, 플러그인 소유 프롬프트·플러시·도구·런타임, 호스트 SDK, LanceDB와 외부 엔진                                      | 대안 저장소는 도구와 포획 의미가 달랐으므로 코어 프롬프트와 수명주기가 선택된 소유자를 따라야 했다.                     |
| 평면 메모가 모든 지식 문제를 표현할 수 없다     | 일별 메모와 선별된 요약                                      | 출처, 주장, 백링크, 린트를 갖춘 선택적 Memory Wiki                                                                                  | 구조화된 주장과 모순은 가치 있지만, 낮은 작성 부담이 중요한 모든 개인 메모에 적용하기에는 너무 무겁다.                  |
| 사이드카 파일은 어긋난다                        | 독립 JSON/JSONL 상태와 단독 인덱스                           | 공유·에이전트별 SQLite, 진단 도구 소유 마이그레이션, 접근자 기반 대화 기록 코퍼스                                                   | 여러 운영 저장소는 식별 정보, 트랜잭션, 정리, 마이그레이션, 권한 부여를 모호하게 만들었다.                              |
| 대화 아카이브에 접근하기 어렵다                 | 수동 `session-logs` 검색, 그 뒤 실험적 임베딩 기반 세션 소스 | 정식 SQLite 대화 기록, `sessions_search`, 검색 후 가시성을 적용하는 인덱싱된 대화 기록 코퍼스                                       | 정확한 아카이브 조회와 표면 간 연속성이 셸·저장소 배치 탐색이나 임베딩을 요구해서는 안 되었다.                          |
| 넓은 회상은 컨텍스트를 유출할 수 있다           | main/direct 의도를 주로 템플릿으로 표현                      | 에이전트별·비공개 대화 기록 필터와 격리를 고려한 기본값                                                                             | 표면을 넘는 기억은 다른 에이전트, 그룹, 보조자, 식별 불가 대상을 제외할 때만 유용하다.                                  |
| 검색된 글이 안전하지 않은 행동을 촉발할 수 있다 | 메모리를 사실·선호로 설명                                    | 공식 권한, 승인, 만료, 행동 허용 지침                                                                                               | 관련성은 권한 부여가 아니다. 오래된 논의를 현재 허가로 바꾸어서는 안 된다.                                              |
| 통합에 신호가 부족하다                          | 사람·하트비트가 일별 메모를 정제                             | 회상 관찰과 Dreaming 승격. 다만 Dreaming은 기본 비활성이라 하트비트 정제를 대체하지 않고 병존한다                                   | 반복 검색은 효용의 대리 지표를 제공하지만, 현재 피드백 순환에는 노출 보정, 부정 신호, 통제된 향상도 시험이 더 필요하다. |

열 행을 가로질러 보면 같은 방향이 되풀이된다. 프롬프트만으로 기대하던 동작에는 런타임 소유자가 생겼고, 정체가 모호한 파일 상태에는 데이터베이스 소유자와 마이그레이션 경계가 생겼으며, 넓어진 회상에는 식별 정보와 범위 조건이 따라붙었다. 그 흐름을 세 가지 원칙으로 압축할 수 있다.

## 변화의 이유는 어디에서 확인할 수 있는가

같은 코드 변경이라도 “왜”를 아는 방법은 서로 다르다. 어떤 변화는 사용자가 불편을 구체적으로 보고했고, 어떤 변화는 유지보수자가 원하는 제품 경계를 PR에 적었다. 실제 장애나 폴백이 계기가 된 경우도 있다. 반대로 커밋의 구조만 분명하고 저자의 설명은 남아 있지 않은 변화도 있다. 이 책은 그 차이를 지우지 않는다.

가장 강한 경우는 **명시적 요청이나 사건이 변경과 직접 연결된 때**다. [이슈 #102](https://github.com/openclaw/openclaw/issues/102)의 검색·승격 요청 자체는 명시적이지만, 후속 벡터 검색과 Dreaming이 이 이슈에 답했다는 직접 연결은 없어 기능적 대응 관계로만 다룬다. 반면 [이슈 #6877](https://github.com/openclaw/openclaw/issues/6877)은 플러시가 일일 파일을 덮어쓴 실제 손실과 수정 PR을, [이슈 #20730](https://github.com/openclaw/openclaw/issues/20730)은 CJK 어휘 검색 실패와 수정 PR을 직접 잇는다. [이슈 #90023](https://github.com/openclaw/openclaw/issues/90023)도 검색 실패가 약 70초의 전경 지연으로 바뀐 사건과 후속 변경을 연결한다. 이 세 경우에는 “사용자가 이런 문제를 겪었고 프로젝트가 이렇게 답했다”고 직접 말할 수 있다.

두 번째는 **PR이나 설계 문서가 선택 이유를 직접 설명한 경우**다. [PR #808](https://github.com/openclaw/openclaw/pull/808)의 논의는 하나의 메모리 플러그인만 기능을 소유해야 한다는 경계를 드러냈다. [PR #3160](https://github.com/openclaw/openclaw/pull/3160)은 QMD의 옵트인·격리·경로 안전·내장 백엔드 폴백 계약을 직접 명시하지만, QMD를 관리형 제품 백엔드로 채택한 더 넓은 선택 이유는 구현 계보에서 좁게 추론한다. [PR #13069](https://github.com/openclaw/openclaw/pull/13069)은 대규모 비동기 배치 처리량보다 보통 크기 재인덱싱의 응답 시간을 우선한 이유를 수치와 함께 설명했다. 마지막 경우처럼 이유까지 적혀 있을 때만 구현 형태와 동기를 모두 명시적 근거로 삼는다.

세 번째는 **코드에서 좁게 추론한 설계 압력**이다. 3월의 소유권 리팩터처럼 프롬프트·플러시·도구·엔진이 선택된 플러그인 쪽으로 연속 이동한 사실은 코드와 테스트로 확정된다. 그러나 모든 커밋에 저자가 남긴 이유가 있는 것은 아니다. 이런 경우 이 책은 “대안 구현을 실제로 교체 가능하게 하려는 소유권 정리로 읽힌다”처럼 관찰된 구조가 허용하는 범위까지만 말한다.

마지막은 **맥락적 신호**다. [이슈 #48217](https://github.com/openclaw/openclaw/issues/48217)의 시작 직후 기억상실 보고와 런타임의 날짜별 노트 주입은 같은 실패 형태를 가리키지만, 구현 PR이 그 이슈를 원인으로 연결하지 않았다. Hindsight가 검색과 승격을 함께 생각하게 한 초기 제안의 배경이 된 것은 확인되지만, Dreaming PR이 Hindsight를 직접 근거로 삼았다는 증거도 없다. 이런 대응 관계는 아이디어의 계보를 설명할 수는 있어도 직접 인과로 쓰지 않는다.

따라서 “폴백 때문에 바뀌었다”는 말도 세분해야 한다. QMD가 없거나 검색에 실패할 때 내장 백엔드로 돌아가는 것은 **현재 코드의 복구 계약**이고, 그 계약의 범위는 PR 설명에서 확인한다. 반면 특정 폴백 사건이 다음 커밋을 직접 촉발했다고 쓰려면 연결된 이슈나 PR 문구가 따로 필요하다. 이 구분을 바탕으로 2장부터 7장은 각 변화 곁에 동기와 근거의 성격을 밝히고, [12장](12-landmark-index.md)은 41개 이정표를 한눈에 대조한다.

## 1. 프롬프트에 대한 희망을 런타임 강제로 바꾸다

연대기에서 가장 뚜렷한 변화는 프롬프트를 더 유려하게 만든 일이 아니었다. 작업의 소유자를 바꾼 일이었다.

- 컴팩션이 전용 플러시 대화를 호출한다.
- 시작 컨텍스트가 정확한 달력 날짜의 메모를 선택한다.
- Active Memory가 주 답변 전에 회상을 실행할지 결정한다.
- 대화 기록 접근은 정식 접근자를 통해 에이전트·세션 식별 정보를 운반한다.
- 오래된 상태는 런타임 폴백이 아니라 진단 도구가 마이그레이션한다.

프롬프트는 여전히 의미 판단에 유용하다. 그러나 이제 런타임이 판단이 **언제**, **어디서**, **어떤 범위 아래에서** 일어날지를 결정한다.

### 모든 런타임 강제는 같은 종류가 아니다

이 이동을 무조건적인 진보로 읽으면 역사적 대응과 앞으로의 설계 원칙이 섞인다. 명시적인 하네스에는 적어도 세 종류가 있다.

| 명시성의 종류       | 메모리에서의 예                                                                 | 모델·작업 분포가 바뀔 때의 기본 태도                                      |
| ------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| 안전·무결성·가용성 불변식 | 선택된 권한 정책의 전 경로 강제, 원자적 쓰기, 삭제 수렴, 승인, 하드 타임아웃 | 모델이 강해져도 유지하고 런타임에서 강제한다.                             |
| 제품 의미 계약      | 무엇을 메모리로 부르는지, 누가 자격이 있는지, 출처·만료·행동 권한의 의미       | 제품 정책으로 명시하고 버전 관리한다. 런타임 불변식은 선택된 정책을 집행한다. |
| 능력 스캐폴딩       | 강제 검색 시점, 고정된 도구 순서, 응답 전 별도 회상, 프롬프트의 절차 예외      | 특정 모델·작업에서 얻는 순효용을 재검증하고 완화·교체·제거할 수 있어야 한다. |

앞의 두 종류는 모델이 어떤 전략을 택해도 지켜야 한다는 뜻에서 **결과 계약**으로 묶을 수 있다. 첫째가 선택된 정책을 모든 경로에서 집행하는 불변식이라면, 둘째는 무엇을 집행할지 정하는 제품 의미다.

하나의 기제가 여러 종류를 함께 담을 수도 있다. 예를 들어 컴팩션 전에 영속 사실이 사라지지 않아야 한다는 요구는 결과 계약이고, 전용 플러시 대화를 쓰는 것은 구현 전략이며, 파일을 먼저 읽고 뒤에 추가하라는 상세 문장은 절차 스캐폴딩이다. 청킹·상위 K·재순위화 같은 검색 기본값도 모델 능력만이 아니라 인덱스 표현, 컨텍스트 예산과 지연 제약을 함께 담을 수 있으므로 이름만 보고 스캐폴딩으로 단정해서는 안 된다.

앞의 두 종류는 모델이 더 능동적으로 행동할수록 오히려 중요해질 수 있다. 반면 능력 스캐폴딩은 당시의 실패를 줄이는 대신 컨텍스트·지연·호출 비용을 늘리고, 모델이 선택할 수 있는 검색·계획 전략을 제한할 수 있다. 이 비용을 이 책에서는 **하네스 세금**이라고 부른다. 비용이 있다는 사실만으로 제거할 수는 없고, 막아 주는 실패와 함께 측정해야 한다.

따라서 책임의 경계는 다음처럼 다시 쓰는 편이 정확하다.

> **런타임은 되돌리기 어렵거나 권한에 민감한 결과와 제품 경계를 소유하고, 모델은 그 경계 안에서 문제 해결 전략을 선택한다.**

예를 들어 기존 내용이 사용자 승인 없이 사라지지 않아야 하고, 쓰기는 원자적이어야 하며, 충돌은 탐지되어야 한다는 요구는 결과 계약이다. 모델이 먼저 읽고 append할지, 구조화된 patch를 만들지, 병합을 요청할지는 전략이다. 권한 없는 기억을 주입하지 않는 것과 인용이 실제 원본을 가리키는 것도 결과 계약이다. 반면 모든 답변 전에 반드시 검색하게 하거나 선택 플러그인이 활성화된 경로에서 Active Memory를 먼저 실행하는 것은 전략 선택이다.

이 구분은 두 종류의 확장성을 함께 보게 한다. 여러 공급자·로컬 모델·채널에서 일정한 최저 동작을 만드는 **운영 확장성**에는 명시적인 스캐폴딩이 유리할 수 있다. 새 모델이 더 나은 검색 시점·도구 순서·검증 방법을 스스로 선택하게 하는 **능력 확장성**에는 같은 스캐폴딩이 상한이 될 수 있다. 하나의 전역 하네스가 두 목표를 언제나 동시에 최적화한다고 가정해서는 안 된다.

현재의 강제 검색이나 고정 파라미터가 이미 불필요해졌다는 결론은 이 역사만으로 낼 수 없다. 반대로 과거에 필요했다는 사실이 앞으로의 필요성을 보장하지도 않는다. 같은 결과 계약과 초기 상태를 유지한 모델×작업×하네스 비교가 필요하다. [9장](09-signal-ledger-and-evaluation.md)은 이런 정책 변경의 증거 기준을, [11장](11-benchmarks-and-roadmap.md)은 스캐폴딩 제거 평가의 실행안을 제안한다.

## 2. 코어 의미 메모리와 기계 상태를 분리하다

초기의 “Markdown은 기준 원본이고 인덱스는 재구축할 수 있다”는 생각은 `memory-core`에서 살아남았다. 달라진 것은 운영 상태의 정의다.

- 청크, 벡터, FTS 행, 리비전, 임베딩 캐시는 파생 인덱스 상태다.
- 회상 관찰, Dreaming 체크포인트, 잠금은 플러그인의 기계 상태다.
- 세션 이벤트는 정식 대화 상태다.
- Markdown은 기본 코어 경로에서 사람이 편집할 수 있는 의미 산출물로 남는다. 대안 플러그인은 별도의 의미 저장소를 소유할 수 있다.

이 분리는 복구를 가능하게 한다. 손상된 인덱스는 다시 만들 수 있고 이전 상태 파일은 옮길 수 있지만, 사용자의 메모를 불투명한 상태로부터 조용히 다시 생성해서는 안 된다.

## 3. 범위는 노출과 학습 전에 강제되지만, 일부는 검색 후 필터다

7월의 대화 간 경로는 서로 다른 범위 경계 두 개를 합성한다. 순서는 다음과 같다.

1. 신뢰된 `corpus=sessions` 회상은 순위화 전에 백엔드 소스 선택을 세션 컬렉션으로 제한할 수 있다.
2. `memory_search`는 그래도 `maxResults`를 백엔드에 전달한다.
3. 백엔드 검색이 끝난 뒤, 해당하는 세션별 사후 필터를 적용한다.
4. 이 필터를 통과한 검색 결과만 인용, 응답 노출, Dreaming 회상 추적으로 간다.

즉 **소스 선택은 백엔드 검색 전**, **세션별 권한 검사는 백엔드 검색 후**다. 두 경계를 하나로 뭉뚱그리면 실제 안전성과 결과 수를 잘못 이해하게 된다.

일반 세션 검색 결과에는 세션 도구 가시성과 에이전트 간 정책이 적용된다. 신뢰된 대화 간 검색 결과에는 다음 조건이 더 필요하다.

- 요청자가 신뢰되며 샌드박스 상태가 아닐 것
- 같은 에이전트 식별 정보일 것
- 앵커와 후보 별칭이 비공개일 것
- 현재 대화 기록이 아닐 것

DM 범위 설정은 기본 활성화와 키 형태에 영향을 주지만, 이 신뢰된 검색 결과별 판정식 자체를 대신하지 않는다. 필터는 인용, 응답 노출, Dreaming 회상 추적 전이지만 백엔드 검색이나 인덱스 뒤에 실행된다. 따라서 차단된 후보도 결과 슬롯을 소비할 수 있다. 현재의 [전체 순서](https://github.com/openclaw/openclaw/blob/a115af277410a91fb039d2ed699eafad706f5c73/extensions/memory-core/src/tools.ts#L620-L759)와 [사후 필터 분기](https://github.com/openclaw/openclaw/blob/a115af277410a91fb039d2ed699eafad706f5c73/extensions/memory-core/src/session-search-visibility.ts#L157-L275)가 이를 보여 준다.

반면 `sessions_search`는 다른 계약을 가진다. 보이는 대화 기록 검색 결과를 먼저 모으고 나서 최종 상한을 적용한다. 이는 [정확 검색 순서](https://github.com/openclaw/openclaw/blob/a115af277410a91fb039d2ed699eafad706f5c73/src/agents/tools/sessions-search-tool.ts#L470-L510)에 명시되어 있다.

따라서 범위는 노출과 학습 전에 강제되지만, 백엔드 검색 단계에 포함되는 것은 소스 선택뿐이다. 세션별 권한 검사는 검색 후에 이뤄진다.

> **예시 — 합성 사례:** `memory_search`가 백엔드 상위 5개를 받은 뒤 두 검색 결과를 차단하면 세 개만 보일 수 있다. 반면 `sessions_search`는 보이는 검색 결과를 모은 뒤 최종 상한을 적용하므로 같은 최종 상한이라도 채우는 방식이 다르다. 이는 실행 순서를 설명하는 가상 예시이며 성능이나 실제 결과 수를 관측했다는 뜻이 아니다.

## 2025년 제안과 2026년 시스템 비교

이제 2025년 제안을 일종의 기준선으로 놓고 2026년 시스템을 다시 본다. 가운데 열은 현재까지 실제로 도달한 결과이고, 마지막 열은 구현 여부와 범위를 평가한다. 특히 기본 메모리 플러그인에서 실현된 원칙을 OpenClaw의 모든 메모리 구성에 보편적인 것으로 읽지 않는 것이 중요하다.

| 제안의 생각                           | 2026-07-18까지의 결과                                                                                     | 평가                                                      |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| Markdown이 기준 원본이다              | 내장·QMD 경로가 보존했고 가져오기가 이동성을 강화했다. 대안 플러그인은 의도적으로 다른 저장소를 소유한다. | `memory-core`에서는 강하게 실현, 전체에 보편적이지는 않음 |
| 파생 SQLite 인덱스는 재구축할 수 있다 | 실현되었고, 나중에는 식별 정보와 원자적 게시를 갖춘 에이전트별 SQLite로 이동했다.                         | 강하게 실현                                               |
| FTS 우선, 임베딩은 나중               | 벡터가 먼저 출시되었고 하이브리드와 견고한 FTS 전용 동작이 뒤따랐다.                                      | 순서가 반대였음                                           |
| 오프라인 우선 운용                    | FTS 전용, 로컬 임베딩, Ollama, QMD를 사용할 수 있다. 초기·기본 의미 검색 설정은 OpenAI를 선호했다.        | 유일한 기본 경로가 아니라 선택지로 실현                   |
| 보존·회상·성찰 순환                   | 수명주기 포획, 메모리 도구, 회상 추적, Dreaming이 이제 이런 순환을 이룬다.                                | 다른 방식으로 실현                                        |
| 형식화된 엔터티·의견·신뢰도           | 내장 의미 메모리에는 포함되지 않았다.                                                                     | 대부분 미실현                                             |
| 출처와 모순                           | 검색 인용이 있으며 선택적 Memory Wiki가 더 풍부한 출처·린트를 추가한다.                                   | 인접 플러그인에서 부분 실현                               |
| 시간 충돌 처리                        | 날짜별 메모, 선택적 감쇠, 세션과 장기 기억의 순위화 시나리오, 행동 민감 지침이 있다.                      | 일급 진실 모델이 아니라 휴리스틱                          |
| 그래프 관계                           | 내장 메모리가 아니라 Memory Wiki 백링크로 존재한다.                                                       | 선택적·인접 기능                                          |
| 처음에는 무거운 SuCo 스택을 피한다    | SuCo/Subspace-Collision ANN 인덱스나 질의 구현을 채택하지 않았다.                                         | 따랐음                                                    |

표의 대비는 선명하다. 제안이 가장 정확했던 부분은 **구조**, 곧 투명한 원본과 파생 검색의 결합이었다. 가장 덜 실현된 부분은 **의미론**, 곧 형식화된 믿음, 신뢰도, 모순, 명시적 권한이었다. 그래서 이 책은 또 하나의 구분 없는 벡터 저장소를 더하는 일보다 이 의미론의 간극을 먼저 평가할 가치가 있다고 본다. 그러나 그것은 역사만으로 확정된 우선순위가 아니다. 결과 비교 증거가 실제 효용과 피해를 검증해야 한다.

## 다음 장으로

설계 원칙은 무엇을 지켜야 하는지는 말해 주지만, 다음 변화가 정말 더 나은지는 판정해 주지 않는다. 런타임이 작업을 맡았다고 해서 결과가 자동으로 좋아지는 것도 아니고, 과거에 유용했던 스캐폴딩이 지금도 필요하다고 말할 수도 없다. 더 많은 메모리가 검색되었다고 해서 미래 작업에 도움이 되었다고 말할 수도 없다.

다음 장에서는 역사 속 버그 보고, QA 카나리아, 인용, 회상 횟수, Dreaming 검토를 하나의 신호 장부로 모은다. 개별 사건이 기존 보장을 반증하는 힘과 후보 정책을 승인하는 힘을 먼저 나누고, 정보가 제시된 순간부터 나중 결과까지 어디에서 기억이 끊겼는지를 구분할 평가 언어를 세운다.

---

[← 이전: 2026년 7월](07-july-2026.md) · [목차](README.md) · [다음: 신호와 평가 전략 →](09-signal-ledger-and-evaluation.md)
